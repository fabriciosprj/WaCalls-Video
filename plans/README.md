# Implementation Plans

Generated by the `improve` skill on 2026-09-17, from a full-repo audit of
WaCalls-Video at commit `3d2279e` (branch `feature/video-nack-e-keyframe`,
working tree had uncommitted changes at audit time — see individual plans'
drift-check sections for which files that affects). Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

These five plans are the top 5 by leverage from a 24-finding audit covering
correctness, security, performance, test coverage, tech debt, dependencies,
DX, docs, and direction. The remaining 19 findings (and 2 direction
suggestions) were reported to the operator but not planned — see "Findings
considered and not (yet) planned" below.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Enforce call ownership on webrtc/reject/end endpoints | P1 | S | none | DONE — `d0cba21` |
| 002  | Close the TOCTOU race in doStartCall (owner reservation) | P1 | S | none | DONE — `5578186` |
| 003  | Synchronize access to activeCall.bridge | P1 | S | none | DONE — `0759d84` |
| 004  | Gate handleVideoNack on the NACK's mediaSSRC | P1 | S | none | DONE — `d2e976b` |
| 005  | Verify the SRTP/SRTCP auth tag on unprotect | P1 | M | none | DONE — `3e0587e` |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

Todos os 5 foram executados e mergeados em `main` (via PR #1) em
2026-09-18. **Nota histórica importante:** planos 004 e 005 mexiam no
motor de chamada customizado (`internal/voip/call`, `internal/voip/media`)
que, LOGO DEPOIS dessa execução, foi substituído pelo
[meowcaller](https://github.com/purpshell/meowcaller) como dependência —
ver `.ai/known-issues.md`. Os commits continuam corretos e no histórico,
mas o código que eles corrigiram não está mais no caminho servido por
`cmd/server`. Plans 001-003 (dono de chamada, TOCTOU, data race no bridge)
seguem valendo — são em `cmd/server`, ainda no caminho ativo.

## Dependency notes

- No plan logically depends on another — all five fix independent bugs.
- **File overlap, not dependency**: plans 001, 002, and 003 all touch
  `cmd/server/httpapi.go` and/or `cmd/server/broker.go`/`session.go`. If
  executed in the same working tree back-to-back, rebase/merge carefully
  between them to avoid clobbering each other's edits — but they can also
  be executed in parallel on separate branches and merged independently,
  since they touch different functions within those shared files.
- Plans 004 and 005 both touch the inbound-RTCP-trust surface
  (`internal/voip/call/callmanager_rtcp.go`'s `handleInboundRtcp` and what
  it calls into) but modify different files (004:
  `internal/voip/call/callmanager_rtcp.go`; 005:
  `internal/voip/media/{srtp,srtcp}.go`) — no file conflict, but a
  reviewer assessing "is inbound RTCP now trustworthy end-to-end" should
  look at both together.
- Plans 004 and 005 both touch code with **uncommitted local changes** on
  the current branch (`callmanager_rtcp.go` for 004, `srtcp.go` for 005) —
  their drift checks are written against the working tree, not just the
  base commit `3d2279e`. If that working tree is committed/changed before
  these plans execute, re-verify the "Current state" excerpts first.

## Findings considered and not (yet) planned

Reported to the operator in the audit summary, not yet turned into a plan
(no rejections — the operator selected the top 5 by leverage and can
request more at any time):

- SEC-03: `-video-dump` logs SRTP/SRTCP session keys (gated behind a
  non-default flag; lower urgency than 001-005).
- TEST-01 through TEST-05, DEBT-01 through DEBT-03: test-coverage and
  tech-debt findings (dead VP8 code, missing tests on `signaling_build.go`/
  `httpapi.go`/client, `CallManager` god object, `VideoDump` instrumentation
  woven into hot paths).
- PERF-01 through PERF-04: SRTP re-deriving AES/HMAC per packet, audio
  meter re-render churn, `Broadcast` per-packet allocation, unconditional
  keyframe double-broadcast.
- DEP-01 through DEP-04: `whatsmeow`/`pion`/`x/crypto`/`x/net`/
  `modernc.org/sqlite` version lag.
- DX-01, DX-02: missing `CLAUDE.md`/`AGENTS.md`; no client lint/test runner.
- DOCS-01: `.gitignore`'s bare `/docs` entry silently drops the whole
  `docs/` directory from git (confirmed dead leftover since the initial
  commit, not intentional).
- Direction: finishing two-way video (outbound rendering) and deciding the
  fate of the no-op `record`/`duration_ms` call-start parameters.

Ask for plans on any of these at any time — say which finding IDs.
